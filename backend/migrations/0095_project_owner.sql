-- Ответственный за проект.
--
-- Заказчик: в шапке чата проекта должен быть ответственный — а такого поля у
-- проекта не было вовсе: были только постановщики отдельных задач. Один человек,
-- к которому идут с вопросами «что по проекту»: показывается в шапке чата и в
-- сведениях, назначается в настройках проекта руководителем.
ALTER TABLE projects
    ADD COLUMN owner_user_id BIGINT NULL REFERENCES users(id) ON DELETE SET NULL;
