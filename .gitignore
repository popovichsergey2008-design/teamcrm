# dependencies / build
node_modules/
dist/
dist-*/
build/
coverage/
*.tsbuildinfo

# secrets & env — НИКОГДА не коммитим
.env
.env.*
!.env.example
# режимы сборки фронта — не секреты, а конфигурация (ТЗ-9, src/platform)
!frontend/.env.mobile-web
!frontend/.env.capacitor

# infra secrets / generated artifacts
infra/secrets/
infra/nginx/certs/
infra/backups/
infra/**/.env

# logs / os
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/

# документация, ТЗ, спеки, бренд, снимки — живут только локально, в публичный репозиторий не попадают
private/
