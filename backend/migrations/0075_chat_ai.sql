-- Слой 6 ТЗ-3: помощник внутри переписки.
--
-- Ответ ИИ ложится в тот же чат, что и сообщения людей: спросили при всех — ответ
-- видят все, и он остаётся в истории разговора. Отдельная панель «спросить ИИ» рядом
-- с чатом сделала бы из помощника инструмент в стороне, хотя он участник разговора.
--
-- Признак нужен, чтобы ответ помощника нельзя было спутать со словами коллеги:
-- в задаче это уже сделано (task_comments.is_ai), здесь то же самое.
ALTER TABLE chat_messages ADD COLUMN is_ai BOOLEAN NOT NULL DEFAULT FALSE;
