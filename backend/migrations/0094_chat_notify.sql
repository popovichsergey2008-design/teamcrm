-- Уведомления на уровне чата (ТЗ-5, этап 5, раздел 39).
--
-- Общий канал компании шумит весь день, и человек либо выключает уведомления
-- целиком — и пропускает личное, — либо терпит. Настройка на каждый чат:
--   all      — как сейчас: звук, всплывашка, счётчик;
--   mentions — только когда позвали по имени;
--   none     — тихо; чат считает непрочитанное для себя, но не для панели.
-- Личная настройка: живёт в строке участия, у каждого своя.
ALTER TABLE chat_members
    ADD COLUMN notify VARCHAR(10) NOT NULL DEFAULT 'all';
