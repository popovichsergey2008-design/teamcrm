-- Виртуальный исполнитель «ИИ-агент»: флаг «задача передана агенту» (v3.2).
-- Не отдельный пользователь — простой признак на задаче; человек-руководитель/ревьюер остаётся.
ALTER TABLE tasks ADD COLUMN agent_assigned BOOLEAN NOT NULL DEFAULT FALSE;
