-- Задача #1368: «Сделаю сегодня» напротив каждой задачи в сводке секретаря.
--
-- Сводка дня показывалась одним куском текста («Просрочено (2): «А», «Б»»), и нажать
-- по ней было нечего — только «Скрыть». При этом подпись над списком обещала, что
-- «Сделаю сегодня» ставит задачу в план: обещание было, кнопки не было.
--
-- Храним рядом с текстом состав сводки: какие задачи в неё попали и какие из них мои
-- (планировать день можно только своей задачей — см. TasksService.setFocusDate).
ALTER TABLE assistant_pings ADD COLUMN IF NOT EXISTS items JSONB NULL;
